server:
  ip: 0.0.0.0
  port: 8000
  http_port: 8003
  websocket: ws://${HOST_IP}:8000/xiaozhi/v1/
  vision_explain: http://${HOST_IP}:8003/mcp/vision/explain
  auth:
    enabled: false

selected_module:
  LLM: ChatGLMLLM
  Memory: nomem
  Intent: nointent
